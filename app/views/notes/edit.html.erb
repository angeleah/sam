<h1>
	<%= @client.first_name %>
	<%= @client.last_name %>
</h1>
<h12>Edit a Note</h2>

<%= form_for [@client, @note] do |f| %>
	<div class="field">
		<%= f.label :note %>
		<%= f.text_field :note %>
	</div>
	<%= f.submit "update note" %>
<% end %>